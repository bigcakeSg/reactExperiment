const Home = (userId, setUserId) => {
  return (
    <>
      <h1>Home Page</h1>
      <input type="text" />
      {/* <input
        type="text"
        value={userId}
        onChange={(e) => setUserId(e.target.value)}
      /> */}
    </>
  );
};

export default Home;
